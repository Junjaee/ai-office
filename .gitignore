# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dev.log
dev.err.log

# env files (can opt-in for committing if needed)
.env*
# Cloudflare 로컬 비밀값 (예시 파일만 커밋)
.dev.vars
!.dev.vars.example

# vercel
.vercel

# typescript
next-env.d.ts
/dist/
/.wrangler/
/outputs/
/work/
node_modules
.wrangler
dist
.next
__pycache__/
*.pyc
.pytest_cache/

# 개인 폴더(설정·메모, 공유 안 함) — 사용자 결정 2026-09-10
/ai-assembly/
/ai-home/
/ai-stock/
/ai-side/
# 비밀값(혹시 폴더에 남아도 절대 커밋 금지)
token.json
client_secret.json
seen_msgs.json
# 드라이브 CLAUDE.md 병합용 백업
CLAUDE.md.drive-old
.claude/settings.local.json

# Claude Code 개인 설정(이 PC 의 권한 허용 목록)
.claude/settings.local.json
# 인스타 자동화 중간 결과(카드 JPEG 등)
/automations/insta/out/
