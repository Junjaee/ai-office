# AI 오피스 대시보드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 갓생맘 AI OFFICE 템플릿을 의원실 자동화 대시보드로 바꾸고, 자동화가 GitHub에 커밋하는 상태 JSON이 부서 상태에 반영되게 한 뒤 Cloudflare Workers에 배포한다.

**Architecture:** 템플릿(Next.js + vinext + Cloudflare Workers)은 `C:\Users\smart\dev\ai-office` git 저장소로 복사한다. 실제 상태는 `public/status/*.json`에서 읽는 순수 규칙 모듈(`app/status-rules.ts`)과 조회 훅(`app/status.ts`)으로 시뮬레이션 상태를 덮어쓴다. 자동화 쪽은 `dev/자동화/common/report_status.py`가 JSON을 쓰고 git push 한다.

**Tech Stack:** Node 22 x64(포터블, `C:\Users\smart\AppData\Local\node-x64\node`), TypeScript/React 19, vinext, wrangler, Python 3.13, pytest, node --test

**설계 문서:** `docs/superpowers/specs/2026-09-09-AI오피스-대시보드-design.md`

**환경 메모:** 사이트 쪽 명령은 모두 `PATH`에 x64 Node를 앞에 붙여 실행한다.
Bash: `export PATH="/c/Users/smart/AppData/Local/node-x64/node:$PATH"; cd /c/Users/smart/dev/ai-office`
템플릿 원본은 `scratchpad/aioffice/ai-office`(node_modules 설치 완료)에 있다.

---

## 파일 구조

```
C:\Users\smart\dev\ai-office\            git 저장소 (템플릿 복사본)
  company.config.ts                       의원실 회사·부서·직원 (전면 교체)
  app/status-rules.ts                     [신규] 상태 JSON → DeptStatus 순수 규칙
  app/status.ts                           [신규] /status/*.json 조회 훅 (60초 폴링)
  app/page.tsx                            [수정] 실제 상태 덮어쓰기, "오류" 상태, 실제/연출 배지, 자동화 현황 패널
  app/game/sim.ts                         [수정] DeptStatus에 "오류" 추가 (타입만)
  app/game/OfficeWorld.tsx                [수정] STATUS_CLASS에 "오류" 추가
  app/globals.css, app/office.css         [수정] .error 색상
  public/status/index.json, minutes.json  [신규] 상태 파일 (초기값)
  tests/status-rules.test.mjs             [신규] 규칙 단위 테스트
  .gitignore                              node_modules, .dev.vars, .wrangler, dev.log
G:\내 드라이브\dev\자동화\common\
  __init__.py, report_status.py           [신규] 상태 JSON 작성 + git push
  tests/test_report_status.py             [신규]
G:\내 드라이브\dev\자동화\01_국회회의록\
  collect_minutes.py, config.yaml         [수정] 실행 끝에 report_status 호출
```

---

### Task 1: 저장소 만들기

**Files:** `C:\Users\smart\dev\ai-office\` 전체, `.gitignore`

- [ ] **Step 1: 템플릿 복사 (node_modules 포함해 시간 절약) 후 git 초기화**

```bash
mkdir -p /c/Users/smart/dev && cp -r "/c/Users/smart/AppData/Local/Temp/claude/G---------------01-------10-----2026----------/f746c921-f552-4798-b42b-89b3c706a382/scratchpad/aioffice/ai-office" /c/Users/smart/dev/ai-office
cd /c/Users/smart/dev/ai-office && rm -f dev.log page.html && cat .gitignore
```

- [ ] **Step 2: .gitignore 확인·보강** — 아래 항목이 없으면 추가

```
node_modules
.dev.vars
.wrangler
dev.log
dist
.next
```

- [ ] **Step 3: git 초기화와 첫 커밋** (사용자 이름·메일은 git 전역 설정이 없으므로 저장소 로컬로 지정)

```bash
cd /c/Users/smart/dev/ai-office && git init -b main && git config user.name "smart" && git config user.email "neverenvy92@gmail.com" && git add -A && git commit -q -m "chore: import 갓생맘 AI OFFICE template" && git log --oneline | head -1
```

Expected: `xxxxxxx chore: import 갓생맘 AI OFFICE template`

---

### Task 2: 상태 규칙 모듈 (app/status-rules.ts)

**Files:**
- Create: `app/status-rules.ts`
- Test: `tests/status-rules.test.mjs`

- [ ] **Step 1: 실패하는 테스트 작성**

```js
// tests/status-rules.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import { deriveDeptStatus, summarize, STALE_HOURS } from "../app/status-rules.ts";

const now = new Date("2026-09-09T15:00:00+09:00");
const base = { id: "minutes", name: "국회회의록 수집", dept: "research", ok: true, running: false,
  updated_at: "2026-09-09T13:38:12+09:00", summary: "신규 0건", counts: { new: 0, replaced: 0, failed: 0, total: 5894 },
  next_run: "매일 09:00", log: [], link: "" };

test("정상이고 최근이면 완료", () => {
  assert.equal(deriveDeptStatus(base, now), "완료");
});

test("실행 중이면 진행 중", () => {
  assert.equal(deriveDeptStatus({ ...base, running: true }, now), "진행 중");
});

test("실패면 오류", () => {
  assert.equal(deriveDeptStatus({ ...base, ok: false }, now), "오류");
});

test("STALE_HOURS 넘게 갱신이 없으면 대기", () => {
  const old = new Date(now.getTime() - (STALE_HOURS + 1) * 3600 * 1000).toISOString();
  assert.equal(deriveDeptStatus({ ...base, updated_at: old }, now), "대기");
});

test("summarize: ops·secretary 집계", () => {
  const all = [base, { ...base, id: "news", dept: "brand", ok: false }];
  const s = summarize(all, now);
  assert.equal(s.total, 2);
  assert.equal(s.ok, 1);
  assert.equal(s.error, 1);
  assert.equal(s.opsStatus, "오류");
  assert.match(s.brief, /2개 팀.*1개 정상.*1개 오류/);
});

test("summarize: 실제 팀이 없으면 대기", () => {
  const s = summarize([], now);
  assert.equal(s.opsStatus, "대기");
  assert.match(s.brief, /연결된 자동화 없음/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `node --test tests/status-rules.test.mjs`
Expected: FAIL — `Cannot find module '.../app/status-rules.ts'`

- [ ] **Step 3: 구현**

```ts
// app/status-rules.ts
// 자동화가 커밋한 상태 JSON을 화면 상태로 바꾸는 순수 규칙. React·fetch 없음.
export type RealStatus = {
  id: string;
  name: string;
  dept: string;
  ok: boolean;
  running?: boolean;
  updated_at: string;
  summary: string;
  counts?: Record<string, number>;
  next_run?: string;
  log?: string[];
  link?: string;
};

export type RealDeptStatus = "완료" | "진행 중" | "오류" | "대기";

/** 이 시간(시간 단위) 넘게 갱신이 없으면 "대기"로 본다 */
export const STALE_HOURS = 36;

export function deriveDeptStatus(s: RealStatus, now: Date = new Date()): RealDeptStatus {
  if (s.running) return "진행 중";
  if (!s.ok) return "오류";
  const updated = Date.parse(s.updated_at);
  if (Number.isNaN(updated) || now.getTime() - updated > STALE_HOURS * 3600 * 1000) return "대기";
  return "완료";
}

export type Summary = {
  total: number;
  ok: number;
  error: number;
  running: number;
  stale: number;
  opsStatus: RealDeptStatus;
  brief: string;
};

export function summarize(all: RealStatus[], now: Date = new Date()): Summary {
  const statuses = all.map((s) => deriveDeptStatus(s, now));
  const count = (k: RealDeptStatus) => statuses.filter((x) => x === k).length;
  const total = all.length;
  const error = count("오류");
  const running = count("진행 중");
  const ok = count("완료");
  const stale = count("대기");
  let opsStatus: RealDeptStatus = "대기";
  if (total > 0) opsStatus = error ? "오류" : running ? "진행 중" : ok ? "완료" : "대기";
  const brief =
    total === 0
      ? "연결된 자동화 없음 — 상태 파일이 올라오면 여기에 표시됩니다."
      : `실제 ${total}개 팀 중 ${ok}개 정상, ${error}개 오류, ${running}개 실행 중, ${stale}개 오래됨`;
  return { total, ok, error, running, stale, opsStatus, brief };
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --test tests/status-rules.test.mjs`
Expected: `# pass 6`

(Node 22.19는 `.ts` 를 타입 제거 방식으로 직접 실행한다. 실패하면 `node --experimental-strip-types --test …` 로 실행한다.)

---

### Task 3: 상태 조회 훅 (app/status.ts)

**Files:**
- Create: `app/status.ts`

- [ ] **Step 1: 구현** (fetch 로직은 브라우저 전용이라 단위 테스트 대신 Task 6에서 화면으로 검증)

```ts
// app/status.ts
"use client";
import { useEffect, useState } from "react";
import { deriveDeptStatus, summarize, type RealDeptStatus, type RealStatus, type Summary } from "./status-rules";

export type RealState = {
  byDept: Record<string, RealStatus>;
  status: Record<string, RealDeptStatus>;
  summary: Summary;
  loadedAt: Date | null;
};

const EMPTY: RealState = { byDept: {}, status: {}, summary: summarize([]), loadedAt: null };
const POLL_MS = 60_000;

export async function fetchRealStatus(): Promise<RealState> {
  const bust = `?t=${Date.now()}`;
  const index = (await (await fetch(`/status/index.json${bust}`)).json()) as { automations: string[] };
  const files = await Promise.all(
    index.automations.map(async (id) => {
      try {
        const r = await fetch(`/status/${id}.json${bust}`);
        if (!r.ok) return null;
        return (await r.json()) as RealStatus;
      } catch {
        return null;
      }
    }),
  );
  const all = files.filter((x): x is RealStatus => Boolean(x && x.id && x.dept));
  const now = new Date();
  const byDept: Record<string, RealStatus> = {};
  const status: Record<string, RealDeptStatus> = {};
  for (const s of all) {
    byDept[s.dept] = s;
    status[s.dept] = deriveDeptStatus(s, now);
  }
  const summary = summarize(all, now);
  status.ops = summary.opsStatus;
  status.secretary = summary.total ? (summary.error ? "오류" : "완료") : "대기";
  return { byDept, status, summary, loadedAt: now };
}

export function useRealStatus(): RealState {
  const [state, setState] = useState<RealState>(EMPTY);
  useEffect(() => {
    let alive = true;
    const load = () =>
      fetchRealStatus()
        .then((s) => alive && setState(s))
        .catch((e) => console.warn("상태 파일 조회 실패", e));
    load();
    const timer = window.setInterval(load, POLL_MS);
    return () => {
      alive = false;
      window.clearInterval(timer);
    };
  }, []);
  return state;
}
```

- [ ] **Step 2: 초기 상태 파일 작성**

`public/status/index.json`
```json
{ "automations": ["minutes"], "generated_at": "2026-09-09T00:00:00+09:00" }
```

`public/status/minutes.json`
```json
{
  "id": "minutes",
  "name": "국회회의록 수집",
  "dept": "research",
  "ok": true,
  "running": false,
  "updated_at": "2026-09-09T13:38:12+09:00",
  "summary": "신규 0건, 확정본 교체 0건, 실패 0건 (1.8분)",
  "counts": { "new": 0, "replaced": 0, "failed": 0, "total": 5894 },
  "next_run": "매일 09:00",
  "log": ["2026-09-09 13:38 신규 0건, 확정본 교체 0건, 실패 0건"],
  "link": ""
}
```

- [ ] **Step 3: 타입 검사**

Run: `npx tsc --noEmit 2>&1 | grep -v -E "db/index.ts|worker/index.ts" | head`
Expected: status.ts·status-rules.ts 관련 오류 없음 (템플릿이 안내한 Cloudflare 타입 오류 3개만 남음)

---

### Task 4: 화면 반영 (sim.ts 타입, OfficeWorld, CSS, page.tsx)

**Files:**
- Modify: `app/game/sim.ts:20`
- Modify: `app/game/OfficeWorld.tsx:17-23`
- Modify: `app/globals.css`, `app/office.css`
- Modify: `app/page.tsx`

- [ ] **Step 1: `sim.ts` DeptStatus에 "오류" 추가**

```ts
export type DeptStatus = "완료" | "진행 중" | "승인 대기" | "연동 대기" | "대기" | "오류";
```

- [ ] **Step 2: `OfficeWorld.tsx` STATUS_CLASS에 추가**

```ts
const STATUS_CLASS: Record<DeptStatus, string> = {
  "완료": "done",
  "진행 중": "working",
  "승인 대기": "approval",
  "연동 대기": "blocked",
  "대기": "waiting",
  "오류": "error",
};
```

- [ ] **Step 3: CSS** — `app/office.css` 끝에 추가

```css
/* 실제 자동화 오류 */
.rm.error { background: #fff1f0; }
.rm-dot.error { background: #ff5c5c; animation: blinkDot .5s steps(2) infinite; }
.lc.error { background: #ffb3b3; }
```

`app/globals.css` 끝에 추가

```css
.error { background: #ffd6d6; }
.status-dot.error, .signal.error, .legend-dot.error { background: #ff5c5c; }
.status-pill.error { background: #ffd6d6; color: #a12020; }
.real-badge { margin-left: 6px; font-size: 10px; padding: 1px 6px; border-radius: 999px; background: #dff7ec; color: #0f6b47; }
.real-badge.sim { background: #eee; color: #666; }
.auto-list { list-style: none; margin: 0; padding: 0; display: grid; gap: 10px; }
.auto-list li { border: 1px solid #eadfe6; border-radius: 12px; padding: 10px 12px; background: #fff; }
.auto-list li b { display: flex; align-items: center; gap: 8px; }
.auto-list li small { color: var(--ink-soft); display: block; margin-top: 4px; font-size: 12px; }
.auto-list li ul { margin: 6px 0 0; padding-left: 16px; font-size: 11px; color: var(--ink-soft); }
```

- [ ] **Step 4: `page.tsx` 수정** — 아래 다섯 군데

(a) import 추가 (기존 import 뒤)
```ts
import { useRealStatus } from "./status";
```

(b) `statusClass`에 "오류" 추가
```ts
const statusClass: Record<DeptStatus, string> = {
  "완료": "done",
  "진행 중": "working",
  "승인 대기": "approval",
  "연동 대기": "blocked",
  "대기": "waiting",
  "오류": "error",
};
```

(c) `Home()` 안, `const [snap, setSnap] = useState…` 바로 아래에 실제 상태를 합친 스냅샷을 만든다. 이후 `snap`을 쓰던 곳은 그대로 두고, **부서 상태를 읽는 곳만** `view`로 바꾼다.
```ts
const real = useRealStatus();
const merged = useMemo<Snapshot>(() => {
  const deptStatus = { ...snap.deptStatus, ...real.status };
  const values = Object.values(deptStatus);
  return {
    ...snap,
    deptStatus,
    stats: {
      done: values.filter((v) => v === "완료").length,
      working: values.filter((v) => v === "진행 중").length,
      approval: values.filter((v) => v === "승인 대기").length,
      blocked: values.filter((v) => v === "연동 대기").length,
    },
  };
}, [snap, real.status]);
```
바꿀 곳: `teams` useMemo의 `snap.deptStatus[room.id]` → `merged.deptStatus[room.id]` (의존성도 `[merged.deptStatus]`), roster의 `snap.deptStatus[room.id]` → `merged.deptStatus[room.id]`, `<OfficeWorld … snap={snap}` → `snap={merged}`, 비서실 카드의 `snap.stats.done` → `merged.stats.done`.

(d) 팀 카드에 실제/연출 배지 — `team-grid`의 `<b>{team.lead.name} · {team.name}</b>` 바로 뒤
```tsx
<span className={`real-badge ${real.byDept[team.id] ? "" : "sim"}`}>
  {real.byDept[team.id] ? "실제" : "연출"}
</span>
```
필터 탭 배열을 `["전체", "진행 중", "완료", "오류", "승인 대기", "연동 대기"]`로, `filter` 상태 타입은 그대로(`DeptStatus`에 "오류"가 들어갔으므로 통과).

(e) 자동화 현황 패널 — `ceo.approval` 섹션(`<section className="two-col">`) 바로 위에 추가
```tsx
<section className="win">
  <div className="win-bar">
    <span>🤖 automation.status</span>
    <span className="window-controls">—　▢　✕</span>
  </div>
  <div className="win-body">
    <div className="section-heading">
      <div>
        <p className="eyebrow">REAL AUTOMATIONS</p>
        <h2>{real.summary.brief}</h2>
        {real.loadedAt ? <small>상태 확인 {real.loadedAt.toLocaleTimeString("ko-KR")}</small> : <small>상태 파일 불러오는 중…</small>}
      </div>
    </div>
    <ul className="auto-list">
      {Object.values(real.byDept).map((s) => (
        <li key={s.id}>
          <b>
            <span className={`status-dot ${statusClass[real.status[s.dept] ?? "대기"]}`} />
            {s.name}
            <span className={`status-pill ${statusClass[real.status[s.dept] ?? "대기"]}`}>{real.status[s.dept]}</span>
          </b>
          <small>{s.summary} · 마지막 실행 {new Date(s.updated_at).toLocaleString("ko-KR")}{s.next_run ? ` · 다음 ${s.next_run}` : ""}</small>
          {s.counts ? <small>누적 {s.counts.total ?? "-"}건 · 신규 {s.counts.new ?? 0} · 교체 {s.counts.replaced ?? 0} · 실패 {s.counts.failed ?? 0}</small> : null}
          {s.log?.length ? <ul>{s.log.slice(0, 5).map((line, i) => <li key={i}>{line}</li>)}</ul> : null}
          {s.link ? <small><a href={s.link} target="_blank" rel="noreferrer">결과 폴더 열기 ↗</a></small> : null}
        </li>
      ))}
    </ul>
  </div>
</section>
```

- [ ] **Step 5: 타입 검사와 테스트**

Run: `npx tsc --noEmit 2>&1 | grep -v -E "db/index.ts|worker/index.ts"; node --test tests/status-rules.test.mjs`
Expected: 새 오류 없음, `# pass 6`

- [ ] **Step 6: 커밋**

```bash
git add -A && git commit -q -m "feat: 실제 자동화 상태를 부서 상태에 반영" && git log --oneline | head -1
```

---

### Task 5: 의원실 회사 설정 (company.config.ts)

**Files:**
- Modify: `company.config.ts` (전면 교체)

- [ ] **Step 1: 파일 교체** — 부서 id·개수·구조는 그대로, 내용만 의원실용으로. `COMPANY`, `CEO_PROFILE`(신준재), `DEPARTMENTS` 12개(설계 3.2 매핑), `STAFF_LIST`(팀마다 lead 1명 + member 1~2명, 의정활동 맥락의 혼잣말, 기존 파스텔 색상 조합 재사용), `PENDING_INTEGRATIONS`(준비 중 9개 부서 → "자동화 준비 중"), `STORAGE_LINK`(드라이브 `_회의록` 폴더 링크는 사용자 확인 전까지 빈 문자열).

부서 매핑(설계 확정):
```
research  국회회의록 수집팀   minutes.lab   📜  회의록 신규분 수집·확정본 교체
brand     뉴스 모니터링팀     news.watch    📰  의원 관련 기사 수집 (준비 중)
strategy1 SNS 모니터링팀      sns.watch     📱  페이스북·인스타 게시글 확인 (준비 중)
qa        상임위 메일팀       mail.desk     ✉️  상임위 메일 확인·텔레그램 알림 (준비 중)
strategy2 질의서 작성팀       brief.write   ✍️  회의 자료 요약·질의서 초안 (준비 중)
reels     영상 제작팀         video.edit    🎬  쇼츠 제작 (준비 중)
carousel  보도자료팀          press.room    🗞️  보도자료 초안 (준비 중)
partner   일정·의사일정팀     schedule.hq   📅  의사일정 정리 (준비 중)
finance   지출·정산팀         ledger.xls    🧾  지출 내역 시트 갱신 (준비 중)
review    자료실 정리팀       archive.box   🗂️  드라이브 자료 분류 (준비 중)
ops       자동화 운영팀       automation.ops ⚙️  실행 성공률·오류 재시도
secretary 비서실              secretary.hq  📋  전 팀 상태 브리핑
```

- [ ] **Step 2: 타입 검사** — `npx tsc --noEmit 2>&1 | grep company.config` → 출력 없음

- [ ] **Step 3: 커밋** — `git commit -qam "feat: 의원실 AI 오피스 회사·부서·직원 설정"`

---

### Task 6: 로컬 화면 검증

**Files:** 없음

- [ ] **Step 1: 개발 서버 실행**

Run (백그라운드): `npm run dev -- --port 3210`
Expected: 20초 안에 `http://localhost:3210/` 200 응답

- [ ] **Step 2: 브라우저 확인** — 인앱 브라우저로 접속해 (1) 회사명·부서명이 바뀌었는지 (2) `automation.status` 패널에 국회회의록 수집이 "완료"로 보이는지 (3) 국회회의록 수집팀 카드에 "실제", 다른 팀에 "연출" 배지가 있는지 스크린샷으로 확인.

- [ ] **Step 3: 오류 상태 시험** — `public/status/minutes.json`의 `"ok": true`를 `false`로 바꾸고 60초 뒤(또는 새로고침) 카드가 "오류"·빨간 불로 바뀌는지 확인 후 원복.

- [ ] **Step 4: 서버 종료**

---

### Task 7: 보고 모듈 (common/report_status.py)

**Files:**
- Create: `G:\내 드라이브\dev\자동화\common\__init__.py` (빈 파일)
- Create: `G:\내 드라이브\dev\자동화\common\report_status.py`
- Test: `G:\내 드라이브\dev\자동화\common\tests\test_report_status.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# common/tests/test_report_status.py
import json
import subprocess
from pathlib import Path

import pytest

from report_status import report, write_status


def git(repo: Path, *args: str) -> str:
    return subprocess.run(["git", "-C", str(repo), *args], capture_output=True, text=True, check=True).stdout


@pytest.fixture
def repo(tmp_path):
    r = tmp_path / "site"
    r.mkdir()
    git(r, "init", "-q", "-b", "main")
    git(r, "config", "user.name", "t")
    git(r, "config", "user.email", "t@t")
    (r / "public" / "status").mkdir(parents=True)
    (r / "public" / "status" / "index.json").write_text('{"automations": []}', encoding="utf-8")
    git(r, "add", "-A")
    git(r, "commit", "-qm", "init")
    return r


def test_write_status_creates_file_and_updates_index(repo):
    path = write_status(repo, "minutes", {"id": "minutes", "name": "국회회의록 수집", "dept": "research",
                                          "ok": True, "summary": "신규 1건"})
    data = json.loads(path.read_text(encoding="utf-8"))
    assert data["id"] == "minutes" and data["updated_at"].endswith("+09:00")
    index = json.loads((repo / "public" / "status" / "index.json").read_text(encoding="utf-8"))
    assert index["automations"] == ["minutes"]
    write_status(repo, "minutes", {"id": "minutes", "name": "x", "dept": "research", "ok": False, "summary": ""})
    index = json.loads((repo / "public" / "status" / "index.json").read_text(encoding="utf-8"))
    assert index["automations"] == ["minutes"]          # 중복 없음


def test_report_commits_and_survives_push_failure(repo, capsys):
    ok = report(repo, automation_id="minutes", name="국회회의록 수집", dept="research", ok=True,
                summary="신규 1건", counts={"new": 1}, next_run="매일 09:00", log_lines=["a", "b"], link="")
    assert ok is False                                   # 원격이 없어 push 실패
    assert "status: minutes" in git(repo, "log", "--oneline", "-1")
    assert "push 실패" in capsys.readouterr().err


def test_report_with_remote_pushes(repo, tmp_path):
    bare = tmp_path / "remote.git"
    subprocess.run(["git", "init", "-q", "--bare", "-b", "main", str(bare)], check=True)
    git(repo, "remote", "add", "origin", str(bare))
    git(repo, "push", "-q", "-u", "origin", "main")
    ok = report(repo, automation_id="minutes", name="국회회의록 수집", dept="research", ok=True,
                summary="신규 2건", counts={}, next_run="", log_lines=[], link="")
    assert ok is True
    assert "status: minutes" in subprocess.run(["git", "-C", str(bare), "log", "--oneline", "-1"],
                                                capture_output=True, text=True).stdout
```

- [ ] **Step 2: 실패 확인**

Run: `cd "/g/내 드라이브/dev/자동화/common" && python -m pytest -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'report_status'`

- [ ] **Step 3: 구현**

```python
# common/report_status.py
"""자동화 실행 결과를 AI 오피스 저장소의 상태 JSON으로 쓰고 git push 한다.

사용:
    from report_status import report
    report(REPO, automation_id="minutes", name="국회회의록 수집", dept="research", ok=True,
           summary="신규 3건", counts={"new": 3}, next_run="매일 09:00", log_lines=[...], link="")

push 실패는 자동화를 멈추지 않는다(경고만 출력, False 반환). 다음 실행 때 다시 push 된다.
"""
from __future__ import annotations

import json
import subprocess
import sys
from datetime import datetime, timedelta, timezone
from pathlib import Path

KST = timezone(timedelta(hours=9))
STATUS_DIR = Path("public") / "status"


def _git(repo: Path, *args: str, check: bool = True) -> subprocess.CompletedProcess:
    return subprocess.run(["git", "-C", str(repo), *args], capture_output=True, text=True,
                          encoding="utf-8", errors="replace", check=check)


def write_status(repo: Path, automation_id: str, data: dict) -> Path:
    """상태 파일과 index.json을 쓴다. updated_at은 항상 지금(KST)으로 채운다."""
    d = repo / STATUS_DIR
    d.mkdir(parents=True, exist_ok=True)
    payload = {**data, "id": automation_id, "updated_at": datetime.now(KST).isoformat(timespec="seconds")}
    path = d / f"{automation_id}.json"
    path.write_text(json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8")
    index_path = d / "index.json"
    try:
        index = json.loads(index_path.read_text(encoding="utf-8"))
    except (FileNotFoundError, json.JSONDecodeError):
        index = {"automations": []}
    ids = [x for x in index.get("automations", []) if x != automation_id] + [automation_id]
    index_path.write_text(json.dumps({"automations": sorted(ids), "generated_at": payload["updated_at"]},
                                     ensure_ascii=False, indent=2), encoding="utf-8")
    return path


def report(repo: str | Path, *, automation_id: str, name: str, dept: str, ok: bool, summary: str,
           counts: dict | None = None, next_run: str = "", log_lines: list[str] | None = None,
           link: str = "", running: bool = False) -> bool:
    """상태 기록 → 커밋 → push. push까지 성공하면 True."""
    repo = Path(repo)
    if not (repo / ".git").exists():
        print(f"[report_status] 저장소가 없습니다: {repo}", file=sys.stderr)
        return False
    _git(repo, "pull", "--rebase", "-q", check=False)      # 원격이 없으면 조용히 실패
    write_status(repo, automation_id, {
        "name": name, "dept": dept, "ok": ok, "running": running, "summary": summary,
        "counts": counts or {}, "next_run": next_run, "log": (log_lines or [])[:5], "link": link,
    })
    _git(repo, "add", str(STATUS_DIR))
    stamp = datetime.now(KST).strftime("%Y-%m-%d %H:%M")
    commit = _git(repo, "commit", "-qm", f"status: {automation_id} {stamp}", check=False)
    if commit.returncode not in (0, 1):                     # 1 = 변경 없음
        print(f"[report_status] commit 실패: {commit.stderr.strip()}", file=sys.stderr)
        return False
    push = _git(repo, "push", "-q", check=False)
    if push.returncode != 0:
        print(f"[report_status] push 실패 (다음 실행 때 재시도): {push.stderr.strip()[:200]}", file=sys.stderr)
        return False
    return True
```

- [ ] **Step 4: 통과 확인**

Run: `cd "/g/내 드라이브/dev/자동화/common" && python -m pytest -q`
Expected: `3 passed`

---

### Task 8: 수집기에 보고 붙이기

**Files:**
- Modify: `01_국회회의록/collect_minutes.py`
- Modify: `01_국회회의록/config.yaml`
- Modify: `01_국회회의록/tests/test_cli.py`

- [ ] **Step 1: 실패하는 테스트 추가** (test_cli.py 끝)

```python
def test_daily_reports_status_when_configured(tmp_path, monkeypatch):
    calls = []
    monkeypatch.setattr(collect_minutes, "report_status", lambda repo, **kw: calls.append((repo, kw)) or True)
    cfg = tmp_path / "config.yaml"
    cfg.write_text(yaml.safe_dump(dict(drive_root=str(tmp_path / "root"), current_th=22, recent_sessions=2,
                                       request_delay=0, retries=1, timeout=5, min_free_gb=0,
                                       office_repo=str(tmp_path / "site"), drive_link="https://drive/x"),
                                  allow_unicode=True), encoding="utf-8")
    assert collect_minutes.run(["--daily", "--config", str(cfg)], site=FakeSite()) == 0
    assert len(calls) == 1
    repo, kw = calls[0]
    assert repo == str(tmp_path / "site")
    assert kw["automation_id"] == "minutes" and kw["dept"] == "research" and kw["ok"] is True
    assert kw["counts"]["new"] == 1 and kw["counts"]["total"] == 2
    assert kw["link"] == "https://drive/x"
```

- [ ] **Step 2: 실패 확인** — `python -m pytest tests/test_cli.py -q` → `AttributeError: … has no attribute 'report_status'`

- [ ] **Step 3: 구현** — `collect_minutes.py`

import 아래에 추가:
```python
sys.path.insert(0, str(HERE.parent / "common"))
try:
    from report_status import report as report_status   # noqa: E402
except ImportError:                                      # common 폴더가 없으면 보고 없이 동작
    report_status = None
```
`run()` 끝, `store.log_run(...)` 다음에 추가 (dry-run 제외):
```python
    repo = (cfg.get("office_repo") or "").strip()
    if repo and report_status and not args.dry_run:
        total_ok = sum(1 for v in store.manifest.values() if v.get("status") == "ok")
        report_status(
            repo, automation_id="minutes", name="국회회의록 수집", dept="research",
            ok=counts["error"] == 0, summary=summary,
            counts={"new": counts["new"], "replaced": counts["replaced"], "failed": counts["error"], "total": total_ok},
            next_run=cfg.get("next_run", "매일 09:00"), log_lines=[summary] + lines[:4],
            link=cfg.get("drive_link", ""),
        )
```
`config.yaml`에 추가:
```yaml
# AI 오피스 대시보드 보고 (비우면 보고하지 않음)
office_repo: "C:/Users/smart/dev/ai-office"
drive_link: ""
next_run: "매일 09:00"
```

- [ ] **Step 4: 통과 확인** — `python -m pytest -q` → `31 passed`

---

### Task 9: 실제 연동 검증과 배포 준비

**Files:** 없음 (실행·안내)

- [ ] **Step 1: 수집기 실제 실행으로 상태 커밋 확인**

Run: `python collect_minutes.py --daily`
Expected: 마지막에 `[report_status] push 실패 (다음 실행 때 재시도)` (원격 미설정이라 정상), `git -C /c/Users/smart/dev/ai-office log --oneline -1` 에 `status: minutes …`

- [ ] **Step 2: 빌드 검증** — `npm run build` 성공, `npm test` (템플릿 테스트) 통과 여부 기록

- [ ] **Step 3: 사용자 안내 (계정 작업)**
  1. GitHub에서 비공개 저장소 `ai-office` 생성 → 주소 전달
  2. 제가 `git remote add origin … && git push -u origin main` 실행 (브라우저 로그인 창은 사용자가 처리)
  3. Cloudflare 대시보드 → Workers & Pages → Create → Import from GitHub → `ai-office` 선택 → Build command `npm run build`, Deploy command `npx wrangler deploy` → 배포 주소 확인
  4. 이후 수집기 실행마다 push → 자동 재배포
